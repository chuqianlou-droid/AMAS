# Quest3 真机遥操作实验流程

前提：已经按 `真实机械臂控制` 完成前面启动：

1. `cr_robot_ros2 dobot_bringup_ros2.launch.py` 正常运行。
2. `dobot_moveit dobot_moveit.launch.py` 正常运行。
3. 原来的 `test_cartesian_execute.py` 或 `test_moveit_pose.py` 能控制真机规划执行。
4. 不要同时运行旧的 `cartesian_ik_teleop`、`cartesian_servo_teleop`。

说明：
- `moveit_pose_controller.py` 不是单独启动的节点。
- 它由 `quest3_cr5_teleop.py` 导入，在终端 3 的 `quest3_cr5_teleop` 进程里运行。
- 终端 2 启动的 `dobot_moveit` 提供 MoveIt 服务 `/compute_cartesian_path`。
- 终端 3 启动的新遥操作节点负责调用 `moveit_pose_controller.py` 做规划和执行。

## 1. 编译

```bash
cd ~/dobot_ws
colcon build --packages-select cr5_teleop --symlink-install
source ~/dobot_ws/install/setup.bash
```

## 2. 启动新遥操作节点，先 dry_run 规划测试

终端 3：

```bash
source /opt/ros/humble/setup.bash
source ~/dobot_ws/install/setup.bash

ros2 launch cr5_teleop quest3_cr5_teleop.launch.py
```

这个 launch 会启动：

```text
quest_udp_receiver
quest3_cr5_teleop
adb shell monkey -p com.sjtu.questcr5teleop 1
```

默认不会在终端 3 逐帧打印手柄坐标，避免刷屏。

需要看手柄坐标时另开终端：

```bash
ros2 run cr5_teleop quest_pose_monitor
```

如果想像日志一样每行输出，不使用单行刷新：

```bash
ros2 run cr5_teleop quest_pose_monitor --ros-args -p one_line:=false -p print_rate:=5.0
```

或者启动时打开 UDP 坐标日志：

```bash
ros2 launch cr5_teleop quest3_cr5_teleop.launch.py udp_log_received_pose:=true
```

看到下面日志，说明 Quest 位姿已经进来：

```text
First Quest3 pose received
```

终端 4 启用：

```bash
source /opt/ros/humble/setup.bash
source ~/dobot_ws/install/setup.bash

ros2 service call /cr5_teleop/set_enabled std_srvs/srv/SetBool "{data: true}"
```

默认 `dry_run=true`，机械臂不会动。正常日志：

```text
Teleop enabled
Cartesian path fraction=...
dry_run=true: planned target accepted, not executing.
```

暂停：

```bash
ros2 service call /cr5_teleop/set_enabled std_srvs/srv/SetBool "{data: false}"
```

## 3. 真机执行

停止终端 3 的 dry_run launch，然后重新启动：

```bash
source /opt/ros/humble/setup.bash
source ~/dobot_ws/install/setup.bash

ros2 launch cr5_teleop quest3_cr5_teleop.launch.py \
  dry_run:=false \
  position_scale:=0.10 \
  command_rate:=1.0 \
  min_target_delta:=0.001 \
  max_step_translation:=0.005 \
  max_total_translation:=0.08 \
  workspace_min_x:=-0.70 \
  workspace_max_x:=0.70 \
  workspace_min_y:=-0.70 \
  workspace_max_y:=0.35 \
  workspace_min_z:=0.05 \
  workspace_max_z:=0.80 \
  max_joint1_delta_deg:=30.0
```

注意：这里不要写 `--ros-args -p dry_run:=false`。  
launch 参数必须写成 `dry_run:=false`。

终端 3 启动日志必须显示：

```text
dry_run=False
```

终端 4 启用：

```bash
ros2 service call /cr5_teleop/set_enabled std_srvs/srv/SetBool "{data: true}"
```

移动 Quest 手柄后，正常日志：

```text
Cartesian path fraction=...
Trajectory execution succeeded.
```

暂停：

```bash
ros2 service call /cr5_teleop/set_enabled std_srvs/srv/SetBool "{data: false}"
```

## 4. 真机无反应时只看这几项

如果终端 3 看到：

```text
dry_run=true: planned target accepted, not executing.
```

说明仍然是 dry_run，启动命令错了，或者没有 source 最新 install。

重新执行：

```bash
cd ~/dobot_ws
colcon build --packages-select cr5_teleop --symlink-install
source ~/dobot_ws/install/setup.bash
```

如果终端 3 看到：

```text
Target clamped to workspace
Trajectory rejected: joint1 total delta ... exceeds ...
```

说明不是程序没运行，而是安全限制拒绝了目标。使用上面真机执行命令里的 workspace 和 `max_joint1_delta_deg:=30.0`。

如果没有 `First Quest3 pose received`：

```bash
ros2 topic echo /quest/right_controller_pose --once
```

如果有 `Cartesian path fraction`，但没有 `Trajectory execution succeeded`：

```bash
ros2 action list | grep follow_joint_trajectory
```

确认真实 action 名是否是：

```text
/cr5_group_controller/follow_joint_trajectory
```

如果不是，用实际 action 覆盖：

```bash
ros2 launch cr5_teleop quest3_cr5_teleop.launch.py \
  dry_run:=false \
  trajectory_action:=/实际控制器/follow_joint_trajectory
```

## 5. 代码位置

```text
cr5_teleop/quest3_cr5_teleop.py
cr5_teleop/moveit_pose_controller.py
config/quest3_cr5_teleop.yaml
launch/quest3_cr5_teleop.launch.py
```

## 6. 低延迟 ServoP 实验版

MoveIt 规划版会有明显延迟和卡顿，因为它每次都要规划轨迹并通过 action 执行。

低延迟版本使用 Dobot V4 的 `ServoP` 动态跟随指令，不走 MoveIt 规划：

```bash
source /opt/ros/humble/setup.bash
source ~/dobot_ws/install/setup.bash

ros2 launch cr5_teleop quest3_cr5_servop_teleop.launch.py \
  position_scale:=0.20 \
  command_rate:=10.0 \
  servo_t:=0.10 \
  min_target_delta_mm:=0.0 \
  raw_target_filter_ratio:=0.80 \
  target_deadband_mm:=2.0 \
  max_speed_mm_s:=50.0 \
  max_accel_mm_s2:=250.0 \
  max_total_translation_mm:=120.0 \
  servo_gain:=200.0 \
  axis_map_robot_x:=vr_x \
  axis_sign_robot_x:=-1.0 \
  axis_map_robot_y:=vr_z \
  axis_sign_robot_y:=-1.0 \
  axis_map_robot_z:=vr_y \
  axis_sign_robot_z:=1.0
```

调试目标点和跳过原因时，在启动命令最后加：

```bash
  log_targets:=true
```

启用：

```bash
ros2 service call /cr5_teleop/servop/set_enabled std_srvs/srv/SetBool "{data: true}"
```

暂停：

```bash
ros2 service call /cr5_teleop/servop/set_enabled std_srvs/srv/SetBool "{data: false}"
```

这个版本的控制逻辑：

```text
Quest 位姿相对增量 -> 坐标轴映射 -> 原始目标点 -> 输入滤波/死区 -> 速度/加速度规划 -> 固定 t 下发 ServoP 点位
```

启动时通过 `GetPose` 获取真机当前笛卡尔位姿，后续固定姿态，只控制 XYZ。

主要参数：

```text
command_rate: ServoP 发送频率，默认 10 Hz
servo_t: ServoP 固定点位时间，默认 0.10s，应与 command_rate 的周期一致
min_target_delta_mm: 目标变化小于该值时不发送；速度规划模式默认 0
raw_target_filter_ratio: 原始目标点低通滤波旧值权重，OpenTeach 风格默认 0.80
target_deadband_mm: 规划点到目标点误差小于该值时不追，用来过滤手柄静止噪声
max_speed_mm_s: 末端最大追踪速度
max_accel_mm_s2: 末端最大加速度，越小越柔和但延迟越大
max_total_translation_mm: 相对启用点的最大位移
servo_gain: ServoP 跟随增益，抖动明显时先降低
axis_map / axis_sign: Quest 左手系到机械臂右手系的坐标映射
```

如果 service 名称不同，先查：

```bash
ros2 service list | grep ServoP
ros2 service list | grep GetPose
```

再覆盖：

```bash
ros2 launch cr5_teleop quest3_cr5_servop_teleop.launch.py \
  get_pose_service:=/实际/GetPose \
  servop_service:=/实际/ServoP
```
